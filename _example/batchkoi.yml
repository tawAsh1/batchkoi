region: ap-northeast-1
job_definition: jobdef.jsonnet
# job_queue: my-job-queue   # default queue for `batchkoi run` (or pass --queue)

plugins:
  - name: tfstate
    config:
      path: terraform.tfstate
  # - name: ssm   # enables ssm('/path') — resolves SSM parameters at render time
